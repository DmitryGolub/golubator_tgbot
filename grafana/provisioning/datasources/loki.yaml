apiVersion: 1

datasources:
  - name: Loki
    uid: loki-logs
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      httpHeaderName1: X-Scope-OrgID
    secureJsonData:
      httpHeaderValue1: ${LOKI_TENANT_ID}
